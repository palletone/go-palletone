*** Keywords ***
NormalNode
    [Arguments]    ${normalNode}
    ${one}    Create List    normalNode    ${normalNode}
    ${two}    Create List    ${one}
    ${result}    创建公用的POST请求    ${depositContractQuery}    normalNode    ${two}
    ${addressMap}    To Json    ${result}
    [Return]    ${addressMap}
