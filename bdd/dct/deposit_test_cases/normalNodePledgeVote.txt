*** Settings ***
Resource          publicParams.txt

*** Keywords ***
normalNodePledgeVote
    [Arguments]    ${normalNode}    ${votes}
    ${params}    params    ${normalNode}    ${votes}
    ${result}    创建公用的POST请求    ${depositContractInvoke}    normalNodePledgeVote    ${params}
    sleep    8
    [Return]    ${result}

params
    [Arguments]    ${normalNode}    ${votes}
    ${one}    Create List    normalNodePledgeVote    ${normalNode}
    ${two}    Create List    ${normalNode}    ${depositContractAddress}    ${votes}    ${fee}    ${one}
    [Return]    ${two}
