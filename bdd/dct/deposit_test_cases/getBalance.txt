*** Settings ***
Resource          publicParams.txt

*** Keywords ***
getBalance
    [Arguments]    ${address}
    ${two}    Create List    ${address}
    ${result}    创建公用的POST请求    wallet_getBalance    wallet_getBalance    ${two}
    ${len}    Get Length    ${result}
    ${amount}    Set Variable If    ${len}==0    0    ${result["PTN"]}
    #    ${result["PTN"]}
    #    Set Variable    ${result["PTN"]}
    [Return]    ${amount}
