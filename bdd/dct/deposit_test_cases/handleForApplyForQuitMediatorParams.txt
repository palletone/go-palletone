*** Settings ***
Resource          publicParams.txt

*** Keywords ***
基金会处理申请退出Mediator列表的所需参数
    [Arguments]    ${foundationAddr}    ${mediatorAddr}    ${isAgree}
    ${one}    Create List    HandleForApplyQuitMediator    ${mediatorAddr}    ${isAgree}
    ${two}    Create List    ${foundationAddr}    ${foundationAddr}    ${amount}    ${fee}    ${one}
    log    ${two}
    [Return]    ${two}

handleForApplyForQuitMediator
    [Arguments]    ${foundationAddr}    ${mediatorAddr}    ${isAgree}
    ${基金会处理申请退出Mediator列表的所需参数}    基金会处理申请退出Mediator列表的所需参数    ${foundationAddr}    ${mediatorAddr}    ${isAgree}
    ${result}    创建公用的POST请求    ${depositContractInvoke}    HandleForApplyForQuitMediator    ${基金会处理申请退出Mediator列表的所需参数}
    sleep    8
    [Return]    ${result}
